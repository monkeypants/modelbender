blockdiag {
  {{ resource }}
  // standard states{-% for state in resource.statechart.get_states() %-}
  {-{ state.name }-}xxx [shape=roundbox];{-% endfor %-}
  // constructors...
  // destructors...
  // transitions...
}
