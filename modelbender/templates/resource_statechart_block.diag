blockdiag {
   orientation=portrait;
   {{ resource }} [shape=roundedbox];
   start [shape=beginpoint];
   end [shape=endpoint];
   start -> {{ resource }} [label=create];
   {{ resource }} -> {{ resource }} [label=update];
   {{ resource }} -> end [label=delete];
}

